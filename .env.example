# Exemplo de arquivo .env para configuração local e produção
# Copie para .env e ajuste os valores

# Flask
FLASK_ENV=development
SECRET_KEY=your-secret-key-here-generate-with-secrets-token-hex

# Database - Supabase PostgreSQL
# Obtenha a connection string no dashboard do Supabase:
# Project Settings > Database > Connection String > URI
POSTGRES_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres
# Ou use DATABASE_URL como fallback
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres

# ============================================
# CONFIGURAÇÃO PARA PRODUÇÃO NA VERCEL
# ============================================
# No dashboard da Vercel (vercel.com/dashboard):
# 1. Vá em Settings > Environment Variables
# 2. Adicione as seguintes variáveis:
#
# FLASK_ENV=production
# SECRET_KEY=<gere com: python -c "import secrets; print(secrets.token_hex(32))">
# POSTGRES_URL=<copie do Supabase: Project Settings > Database > Connection String > URI>
# DATABASE_URL=<mesma URL do POSTGRES_URL como fallback>
#
# ============================================
# COMO OBTER A URL DO SUPABASE
# ============================================
# 1. Acesse: https://supabase.com/dashboard
# 2. Selecione seu projeto
# 3. Vá em: Settings (ícone de engrenagem) > Database
# 4. Em "Connection String", selecione "URI"
# 5. Copie a URL (formato: postgresql://postgres:[password]@db.[ref].supabase.co:5432/postgres)
# 6. Substitua [password] pela senha do seu projeto
