# Exemplo de arquivo .env para configuração local e produção
# Copie para .env e ajuste os valores

# Flask
FLASK_ENV=development
SECRET_KEY=your-secret-key-here-generate-with-secrets-token-hex

# Database - Vercel Postgres
# A Vercel fornece automaticamente a variável POSTGRES_URL quando você conecta um banco
# Para desenvolvimento local, você pode usar PostgreSQL local ou manter SQLite
POSTGRES_URL=postgresql://user:password@localhost:5432/semaforo
# Ou use DATABASE_URL como fallback
DATABASE_URL=postgresql://user:password@localhost:5432/semaforo

# ============================================
# CONFIGURAÇÃO PARA PRODUÇÃO NA VERCEL
# ============================================
# No dashboard da Vercel (vercel.com/dashboard):
# 1. Vá em Settings > Environment Variables
# 2. Adicione as seguintes variáveis:
#
# FLASK_ENV=production
# SECRET_KEY=<gere com: python -c "import secrets; print(secrets.token_hex(32))">
# POSTGRES_URL=<fornecido automaticamente pelo Vercel Postgres>
#
# Nota: POSTGRES_URL é fornecido automaticamente quando você conecta
# um banco Vercel Postgres ao seu projeto
